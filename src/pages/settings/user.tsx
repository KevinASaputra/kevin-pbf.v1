import Container from "@/common/components/elements/Container"

const user = () => {
  return (
    <Container data-aos="fade-up">
      <div>
        <h1>user</h1>
      </div>
    </Container>
  );
}

export default user